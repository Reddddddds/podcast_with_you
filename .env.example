# Cloudflare Worker URL,部署后 worker 给的 https://*.workers.dev 域名
# 例如:VITE_RESOLVE_URL=https://podcast-resolver.xxx.workers.dev
# 注意:留空则走 vite dev proxy -> http://127.0.0.1:8787(wrangler dev)
VITE_RESOLVE_URL=